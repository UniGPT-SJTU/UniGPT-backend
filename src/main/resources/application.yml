spring:
  application:
    name: unigpt
  datasource:
    url: jdbc:mysql://10.119.12.131:3306/unigpt
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    org.springframework.web.socket: DEBUG
